public class math {
    public static void main(String[] args) {
        int result=(int)Math.round(1.1F);
        System.out.println(result);
        double ra=Math.round(Math.random()*100);//round is to round it off and random is to generate random numbers.
        System.out.println(ra);
    }
}
